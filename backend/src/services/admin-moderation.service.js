const userRepository=require('../repositories/user.repository');
const AppError=require('../utils/app-error');
async function listSuspended(){return userRepository.listAdminSuspended();}
async function setStatus(adminId,userId,data){const id=Number(userId);if(!Number.isInteger(id)||id<=0)throw new AppError('Invalid user id.',400,'INVALID_USER_ID');if(id===Number(adminId))throw new AppError('You cannot change your own admin status.',403,'SELF_MODERATION_FORBIDDEN');const status=String(data.status||'').toUpperCase();if(!['ACTIVE','SUSPENDED','BANNED'].includes(status))throw new AppError('Invalid moderation status.',400,'INVALID_MODERATION_STATUS');const reason=String(data.reason||'').trim();if(status!=='ACTIVE'&&!reason)throw new AppError('A reason is required for suspension.',400,'SUSPENSION_REASON_REQUIRED');if(reason.length>500)throw new AppError('Reason must not exceed 500 characters.',400,'SUSPENSION_REASON_TOO_LONG');const until=data.suspendedUntil?new Date(data.suspendedUntil):null;if(until&&!Number.isFinite(until.getTime()))throw new AppError('Invalid suspension expiry.',400,'INVALID_SUSPENSION_EXPIRY');return userRepository.adminSetStatus(require('../config/database').pool,id,status,status==='ACTIVE'?null:reason,status==='SUSPENDED'&&until?until:null);}
module.exports={listSuspended,setStatus};
