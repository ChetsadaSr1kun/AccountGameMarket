function escapeHistoryText(value){return String(value||'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));}
function historyTradeDate(value){if(!value)return '-';const date=new Date(value);return Number.isNaN(date.getTime())?String(value):date.toLocaleString('th-TH');}
function historyTradeStatus(status){if(status==='PAID')return{label:'สำเร็จ',className:'badge-green'};if(status==='PENDING')return{label:'รอชำระเงิน',className:'badge-yellow'};if(status==='COMPLETED')return{label:'สำเร็จ',className:'badge-green'};return{label:status||'ไม่ทราบสถานะ',className:'badge-blue'};}
function renderTradeHistory(orders,currentUserId){const list=document.getElementById('histTradeList');if(!list)return;if(!orders.length){list.innerHTML='<div style="padding:24px;text-align:center;color:var(--muted)">ยังไม่มีประวัติการซื้อ/ขาย</div>';return;}
 list.innerHTML=orders.map(order=>{const isBuyer=Number(order.buyerId)===Number(currentUserId),status=historyTradeStatus(order.status),roleLabel=isBuyer?'ซื้อ':'ขาย',roleClass=isBuyer?'badge-green':'badge-blue',person=isBuyer?`ผู้ขาย: ${escapeHistoryText(order.sellerUsername)}`:`ผู้ซื้อ: ${escapeHistoryText(order.buyerUsername)}`,amount=`${isBuyer?'-':'+'}${Number(order.amount||0).toLocaleString('th-TH')} ฿`,amountColor=isBuyer?'var(--danger)':'var(--success)';let action='';
 if(isBuyer&&order.status==='PAID'){const report=order.hasTransactionReport?'<span class="badge badge-green history-report-badge">✓ รายงานแล้ว</span>':`<button class="btn btn-ghost btn-sm history-report-btn" onclick="openTransactionReportModal(${Number(order.id)})">🚩 รายงาน</button>`;action=`<div style="display:flex;flex-direction:column;align-items:flex-end;gap:6px"><button class="btn btn-ghost btn-sm" onclick="openOrderDetail(${Number(order.id)})">🔐 ดูข้อมูลบัญชี</button>${report}</div>`;}
 else if(order.status==='COMPLETED'){const review= isBuyer?(order.hasReview?'<span class="badge badge-green history-reviewed-badge">✓ รีวิวแล้ว</span>':`<button class="btn btn-primary btn-sm" onclick='openReviewModal(${JSON.stringify(order)})'>⭐ ให้คะแนน</button>`):'';const report=order.hasTransactionReport?'<span class="badge badge-green history-report-badge">✓ รายงานแล้ว</span>':`<button class="btn btn-ghost btn-sm history-report-btn" onclick="openTransactionReportModal(${Number(order.id)})">🚩 รายงาน</button>`;action=`<div style="display:flex;flex-direction:column;align-items:flex-end;gap:6px">${review}${report}</div>`;}
 return `<div class="card history-trade-row" style="display:grid;grid-template-columns:110px minmax(0,1fr) 155px 88px 100px;align-items:center;column-gap:14px;margin-bottom:10px;padding:14px 18px"><div style="display:flex;justify-content:center;align-items:center;justify-self:center"><span class="badge ${roleClass}" style="min-width:64px;text-align:center;display:inline-flex;justify-content:center">${roleLabel}</span></div><div style="min-width:0;text-align:left"><div style="font-weight:600;font-size:14px">🎮 ${escapeHistoryText(order.product?.title||'สินค้า')}</div><div style="color:var(--muted);font-size:12px">${escapeHistoryText(order.product?.gameName||'-')} • ${person} • ${historyTradeDate(order.createdAt)}</div></div><div style="display:flex;justify-content:flex-end;align-items:center;min-width:0">${action}</div><div class="kanit" style="font-weight:700;color:${amountColor};white-space:nowrap;min-width:72px;text-align:right">${amount}</div><div style="width:100px;display:flex;justify-content:center;align-items:center;justify-self:center"><span class="badge ${status.className}" style="min-width:70px;text-align:center;display:inline-flex;justify-content:center">${status.label}</span></div></div>`;}).join('');}
async function loadTradeHistory(){const list=document.getElementById('histTradeList');if(!list)return;list.innerHTML='<div style="padding:24px;text-align:center;color:var(--muted)">กำลังโหลดประวัติซื้อ/ขาย...</div>';try{const r=await fetch('/api/v1/orders',{credentials:'include'}),b=await r.json().catch(()=>({}));if(!r.ok)throw new Error(b.error?.message||'โหลดประวัติซื้อ/ขายไม่สำเร็จ');const orders=b.data?.orders||b.orders||[];renderTradeHistory(Array.isArray(orders)?orders:[],window.currentUser?.id||currentUser?.id);}catch(e){console.error('loadTradeHistory failed:',e);list.innerHTML=`<div class="notice danger">${escapeHistoryText(e.message||'โหลดประวัติซื้อ/ขายไม่สำเร็จ')}</div>`;}}
window.loadTradeHistory=loadTradeHistory;
